STOCK_API_BASE_URL=https://api.example.com
# Default/fallback token (used if specific freezer token is not provided)
STOCK_API_TOKEN=

# Optional tokens per freezer (overrides STOCK_API_TOKEN when set)
STOCK_TOKEN_PAULISTA=
STOCK_TOKEN_BARUERI=
STOCK_TOKEN_CENTRO=

# Local server port (defaults to 4173)
PORT=4173
